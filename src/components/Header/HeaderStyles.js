import { createUseStyles } from 'react-jss';

export default createUseStyles({
  Header: {
    display: 'flex',
    alignItems: 'center',
    height: 50,
    color: 'white',
    backgroundColor: 'tomato',
  },

  logo: {
    display: 'block',
  },

  tel: {
    display: 'block',
  },
});
