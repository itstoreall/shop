import { createUseStyles } from 'react-jss';

export default createUseStyles({
  Footer: {
    display: 'flex',
    alignItems: 'center',
    height: 50,
    color: 'white',
    backgroundColor: 'tomato',
  },

  logo: {
    display: 'block',
  },
});
