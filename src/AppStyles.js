import { createUseStyles } from 'react-jss';

export default createUseStyles({
  App: {
    // backgroundColor: 'skyblue',
  },

  main: {
    display: 'flex',
    width: '100%',
    height: '100vh',
    margin: '0 auto',
  },
});
